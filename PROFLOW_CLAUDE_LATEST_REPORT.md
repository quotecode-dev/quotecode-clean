# PROFLOW — Claude Latest Report

**This file is a REPORT TRANSPORT / REVIEW BRIDGE only.** It is synchronized to the `proflow-continuity` branch, a documentation-only orphan branch verified safe to push (no Vercel deployment consequence). It does **not** replace `PROFLOW_CHAT_HANDOFF.md`, `PROFLOW_HANDOFF.md`, `PROFLOW_TODO.md`, `PROFLOW_PROJECT_CONTEXT.md`, or `PROFLOW_ARCHITECTURE.md`, and it does **not** prove current filesystem/git/runtime state by itself.

**GOLDEN RULE: LATEST CLAUDE REPORT ≠ FRESH LOCAL STATE.** CONTINUITY DOCUMENTS ≠ FRESH LOCAL WORKING TREE either. See `PROFLOW_PROJECT_CONTEXT.md` §17.C/§17.J.

## Task: TEST Runtime Activation Audit — READ-ONLY / PLAN ONLY

**1. Effort level**: HIGH.

**2. Exact continuity ref**: `proflow-continuity` worktree, fetched fresh — `HEAD == origin/proflow-continuity == 0da7ddebe0ae72d16b7627eea0d3e354348d1517` (the exact state §18.CY left), clean, all six `PROFLOW_*.md` files confirmed present and read. No `CONTINUITY BOOTSTRAP INCOMPLETE` condition.

**3. Fresh Local State**: `main`, `HEAD == origin/main == 17ac4d3a950d96f4167f9b320c82b4798382d621` (unchanged). `git status --short`: standing baseline (six tracked docs + `.gitignore` modified, `supabase/migrations/`/two quote-number SQL scripts untracked — all pre-existing). Local migration inventory: 10 files in `supabase/migrations/` (the 6 Quote-Number/Attn-chain files + the 4 retimestamped capture files). Local Edge Function inventory: 9 directories (`admin-cleanup-user-quotes`, `admin-delete-user`, `billing-checkout-stub`, `chat-ai`, `get-public-quote`, `resend-email-webhook`, `send-quote-email`, `send-subscription-expiration-email`, `send-trial-expiration-email`). Env/config inventory: `.env` (5 keys: `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, plus 4 `PROFLOW_TEST_*` credential pairs — names only, no values read/printed) and `.env.example`; no `.env.*.local` mode file exists yet. Supabase CLI link at task start: Production `linked:true`, TEST `linked:false`. Running dev servers: ports 5184 and 5186 both `LISTENING` — command lines confirmed both are plain `node .../vite.js --host --port <5184|5186> --strictPort` with **no `--mode` flag**, so both load the identical default `.env` (pointing at Production). Nothing reset, restored, stashed, cleaned, or discarded.

**4. Current TEST baseline**: target-guarded relink to `quotecode-test` (`supabase projects list` confirmed `linked:true` for TEST / `linked:false` for Production before proceeding). `supabase migration list` showed all 10 local migrations with `local==remote` — no drift. `supabase functions list` returned **zero** functions on TEST. CLI relinked back to Production immediately after and verified restored (`linked:true` again). No SQL row-level re-verification was performed this pass — the migration-history match plus the same-day (§18.CW) exhaustive post-apply verification (schema/constraint/policy/grant inventory, 7-point RLS proof) already established the Files 00-03/Phase 2/Phase 3 PASS baseline, and nothing has touched TEST since (0 Edge Functions confirms no deploy occurred). **No drift found — baseline holds. Nothing repaired (none needed).**

**5. Frontend/backend dependency map**: `src/shared/supabase.js` is the **sole** `createClient()` call anywhere in `src/`, reading only `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` — a single point of control for any future TEST env-split. No hardcoded Supabase project ref or `*.supabase.co` literal exists anywhere else in `src/` (repo-wide grep, zero matches). `src/main.jsx`'s market/bundle selection (Hebrew `AppLocal` vs English `AppGlobal`) is derived purely from query `lang` → path → `localStorage.proflow_lang` → `proflow_geo_country` cookie (written by `middleware.ts`, Vercel-only, never runs on local Vite) → browser language — **entirely independent of which Supabase project or port is active**, meaning one TEST backend can serve both markets without separate infrastructure. Auth: `signInWithPassword` and `resetPasswordForEmail` (in `App.jsx`, `AppGlobal.jsx`, `AppLocal.jsx`, `Dashboard.jsx`) all correctly use `window.location.origin` for redirects — TEST-safe. **Critical finding**: `Dashboard.jsx` line 1320, inside the real `signUp()` call, hardcodes `emailRedirectTo: 'https://www.quotecodepro.com/dashboard'` — deliberate (per an in-code comment: pinned to the canonical domain so confirmation always returns there even from a Vercel preview URL), but means any real signup — from Production, preview, or a future TEST dev server alike — sends the confirmation link to Production's domain, never back to where the signup happened. DB: `business_settings` auto-creates client-side on first authenticated login (`Dashboard.jsx` ~line 746-763), deriving `country`/`currency`/`plan`/`trial_ends_at`/`role` defaults purely from `user_metadata.signup_market` — no Edge Function or extra config required for this step once a confirmed `auth.users` row exists. Storage: client-side direct upload (`supabase.storage.from('quote-files').upload()` + `.getPublicUrl()`) in `Dashboard.jsx`; Public Quote attachment access instead goes through `get-public-quote`'s signed-URL path. `api/geo.js`/`middleware.ts` (Vercel-only) never run under local Vite — a local TEST session always falls through to the manual region-choice prompt rather than auto-detecting country (not a blocker, a UX note).

**6. Edge Function inventory** (full table in `PROFLOW_HANDOFF.md` §18.CZ and `PROFLOW_ARCHITECTURE.md` §1.A):

| Function | Local source | On Prod | On TEST | Frontend caller | DB tables | Storage | Service credential | Market logic | Classification |
|---|---|---|---|---|---|---|---|---|---|
| `get-public-quote` | yes | yes | no | `SmartPublicQuote.jsx` (sole data source for the entire Public Quote page) | `quotes`, `business_settings`, `quote_attachments` | `quote-files` (signed URL) | `SUPABASE_SECRET_KEYS` | no | **A — required for minimum TEST** |
| `send-quote-email` | yes | yes | no | `Dashboard.jsx` | `quotes`, `business_settings`, `clients` | no | `SUPABASE_SECRET_KEYS` + `RESEND_API_KEY` | yes | B — required for full functional test |
| `chat-ai` | yes | yes | no | `AIChatWidget.jsx` | `chat_logs` | no | `SUPABASE_SERVICE_ROLE_KEY` (legacy) + `OPENAI_API_KEY` | yes | C — optional/later |
| `admin-delete-user` | yes | yes | no | `AdminUsersTab.jsx` | `business_settings`, `quotes` | no | `SUPABASE_SERVICE_ROLE_KEY` (legacy) | no | C — optional (admin-only, destructive) |
| `admin-cleanup-user-quotes` | yes | yes | no | `AdminUsersTab.jsx` | `business_settings`, `quotes` | no | `SUPABASE_SECRET_KEYS` | no | C — optional (admin-only) |
| `billing-checkout-stub` | yes | yes | no | none (scaffolding, unwired) | `business_settings` | no | `SUPABASE_SECRET_KEYS` | yes | C — optional, not wired to any UI |
| `resend-email-webhook` | yes | yes | no | Resend's own webhook only | `quotes` | no | `SUPABASE_SECRET_KEYS` + `RESEND_WEBHOOK_SECRET` | no | C — optional, external trigger only |
| `send-trial-expiration-email` | yes | yes | no | Vercel Cron (`api/cron.js`) only | `business_settings` | no | `SUPABASE_SECRET_KEYS` + `RESEND_API_KEY` + `CRON_SECRET` | yes | C — optional, cron-only |
| `send-subscription-expiration-email` | yes | yes | no | Vercel Cron (`api/cron.js`) only | `business_settings` | no | `SUPABASE_SERVICE_ROLE_KEY` (legacy) + `RESEND_API_KEY` + `CRON_SECRET` | yes | C — optional, cron-only |
| `clever-processor` | **no local source** | yes | no | unknown | unknown | unknown | unknown | unknown | **E — blocked/needs redesign; undocumented drift, Owner clarification needed** |
| `send-welcome-email` | **no local source** | yes | no | none found (confirmed not invoked by any signup flow, prior task) | unknown | unknown | unknown | unknown | **D — Production-only, should not be copied as-is; likely orphaned** |

**7. TEST-required Edge Functions + secret/config classification**: for the Minimum Visible Milestone, only `get-public-quote` is required, needing `SUPABASE_URL`/`SUPABASE_SECRET_KEYS`/`SUPABASE_ANON_KEY` — all **must use TEST-specific values** (safe to mirror which *keys* are needed structurally, never the Production *values*). For the Full Functional Milestone: `send-quote-email` additionally needs `RESEND_API_KEY` (Owner decision — a TEST-safe/sandbox key vs. reusing Production's, since Resend is not Supabase-project-scoped); `chat-ai` needs `OPENAI_API_KEY` (structurally safe to mirror since it's not Supabase-project-specific, but Owner should decide whether a separate key avoids TEST usage counting against Production's quota/billing). `CRON_SECRET`/`RESEND_WEBHOOK_SECRET`/`STRIPE_SECRET_KEY` are **unknown/not needed** for any milestone defined here (cron- and webhook-only, or unused scaffolding). No secret value was printed, created, or changed.

**8. Auth configuration findings**: no `[auth]` section exists in `supabase/config.toml` (it only holds per-function deploy flags) — live Site URL/redirect-URL/email-confirmation settings for either project were not queried via the CLI this pass (no safe read-only command was identified for it without risking a values-printing command). The dominant, code-level Auth finding is the `emailRedirectTo` hardcode (item 5 above) — this is a code fact, not a project-config fact, and dominates the practical Auth picture: real signup-with-email-confirmation cannot be meaningfully tested against TEST without either fixing that hardcode or bypassing it. The recommended bypass (Admin-API pre-confirmed user creation) requires **no** Site-URL/redirect-URL configuration on TEST at all, since it never sends a confirmation email. Password-reset redirects (`window.location.origin`-based) are already TEST-safe by construction and would work correctly once the app is actually pointed at TEST. Whether TEST should ever use real email delivery is therefore a **Full Functional Milestone** question only, not a Minimum Visible Milestone one.

**9. TEST user strategy**: no in-app admin user-creation tool exists anywhere (`createUser`/`inviteUserByEmail` not found in `src/`). Recommended: Supabase Admin API, run as a one-off authorized script/action (not through the app UI), `email_confirm: true`, `user_metadata: { signup_market: 'Local' | 'International' }` set explicitly at creation time. This **bypasses the `emailRedirectTo` bug entirely** for both markets — `business_settings` then auto-populates correctly (`country`/`currency`/`plan`/`role`/`trial_ends_at`) on the user's first authenticated Dashboard load, with zero manual DB seeding beyond the `auth.users` row itself. Minimum: 1 Local user, 1 International user. TEST admin/super_admin users only if Super Admin flows specifically need testing — not required for the Minimum Visible Milestone. Never: the Owner's real International account, any real customer, David Aluminum, or any credential resembling them.

**10. International TEST identity strategy**: the previously-documented blocker (the only `country="International"` account anywhere is the Owner's real live account) is **not a technical blocker for a fresh TEST identity** — creating a brand-new Admin-API user directly inside the fully isolated `quotecode-test` project has zero collision surface with the Owner's real account (different Supabase project entirely). The safest alias strategy: a clearly-fictional, clearly-TEST-labeled address (e.g. `proflow-test-intl-1@example.invalid`, matching the existing `fixture-business-*@example.invalid` convention already used in TEST), never resembling `minhatshay@gmail.com` or any real customer address. Email confirmation/reset testing for this identity would work correctly once pointed at TEST (see item 8) but is not required for the Minimum Visible Milestone. **Not created this task.**

**11. Vite/5184/5186 findings**: both ports currently point at Production (no `--mode` flag, both load default `.env`) — confirmed via running-process command lines, not assumed. Local vs. International do **not** need separate ports/backends (see item 5 — market selection is client-side and backend-independent); the previously-*designed* (never-implemented) resolution — a `--mode localtest` + `.env.localtest.local` pointing one port (5186 recommended) at `quotecode-test`, leaving 5184 on Production — remains sound and is now further validated by this audit's dependency map. Risk of a TEST browser session silently hitting Production: currently **zero risk of the reverse is possible but no positive TEST-safety net exists either** — nothing in `src/` currently reports which project is active, so a *future* misconfiguration (e.g. a typo'd env value) would fail silently. Physical-phone/LAN testing is architecturally supported already (same mechanism as the existing 5184 LAN setup, `--host` flag already present on both running instances). No env file was modified, no server was restarted, nothing was rewired this task.

**12. TEST/Production safety-guard plan** (recommendations only, none implemented): (a) `.env.localtest.local` (additive, already covered by the existing `.gitignore` `.env.*.local` pattern) + a `dev:localtest`-style script, never touching `.env` itself; (b) a visible UI banner/watermark when the resolved project ref is TEST's, driven by an env flag — requires its own future, separately-scoped code-change authorization; (c) a lightweight startup assertion in `src/shared/supabase.js` warning if the resolved ref doesn't match the active mode's expectation — same, future/separate; (d) target-guard discipline (fresh `supabase projects list` before/after any TEST mutation) extended to every future Edge Function deploy/secret-set; (e) TEST-only fictional email convention (`@example.invalid`, already established); (f) TEST user naming that never resembles the Owner's real address or any real customer; (g) Edge Function secrets set only while CLI is target-guard-confirmed linked to TEST, values never copied from Production, only which keys are needed.

**13. Minimum Visible Milestone**: Owner opens a browser, logs in as a fictional Local/Hebrew TEST user, sees the Dashboard, creates a quote, and views its Public Quote page — end to end, isolated from Production. Requires: frontend pointed at TEST (Step A), `get-public-quote` deployed to TEST with TEST-specific secrets (Step B), one Local TEST user via Admin API (Step C). DB and Storage need **no further work** — already Phase 2/3 PASS. International is **not required** for this milestone.

**14. Full Functional TEST Milestone** (beyond the minimum): one International TEST user (Step D, same mechanism); `send-quote-email` deployed (+ `RESEND_API_KEY` decision); `chat-ai` deployed (+ `OPENAI_API_KEY` decision) if AI Chat testing is wanted; admin functions only if Super Admin flows need testing; the `storage_path` bug (item 24) fixed if attachment-visibility testing matters; a visible TEST/PROD banner; fixing or permanently documenting the `emailRedirectTo` hardcode if real signup-through-UI testing is ever specifically wanted; `billing-checkout-stub`/cron-based emails/`resend-email-webhook` only if those specific product areas are the thing under test.

**15. Known blockers/issues classification**:
- Zero Edge Functions on TEST — **BLOCKS MINIMUM TEST** until `get-public-quote` is deployed.
- No env-split Vite/`.env` config — **BLOCKS MINIMUM TEST** until created.
- No TEST users exist — **BLOCKS MINIMUM TEST** until at least one is created.
- Hardcoded `emailRedirectTo` (`PROFLOW_PROJECT_CONTEXT.md` §24 item 11, new this task) — **BLOCKS FULL TEST ONLY** (real-signup-UI testing specifically); **does not block** the recommended Admin-API path.
- `storage_path` persistence bug (item 24) — **BLOCKS FULL TEST ONLY** (attachment-visibility testing specifically).
- `quote_number`/Attn architecture — **DOES NOT BLOCK** (already fully captured and applied to TEST).
- `signup_market`/bundle-selection mechanism — **DOES NOT BLOCK** (already market-neutral, TEST-safe, proven).
- Local vs. International account identity — **DOES NOT BLOCK** technically; procedural-discipline note only (see item 10).
- `clever-processor`/`send-welcome-email` undocumented drift — **UNKNOWN**; does not block either milestone; Owner clarification recommended eventually.
- No visible TEST/PROD indicator — **DOES NOT BLOCK** activation itself, but is a real safety gap once activation happens.

**16. HE verdict**: **HE TEST RUNTIME ACTIVATION: PASS WITH CONDITIONS.** Independently re-verified the `emailRedirectTo` and market-routing findings. Additional finding: `api/geo.js`/`middleware.ts` never run locally, so a Local TEST tester always hits the manual region-choice prompt (not a blocker). Condition: fix or reliably work around `emailRedirectTo` before real-UI signup is usable on TEST.

**17. EN verdict**: **EN TEST RUNTIME ACTIVATION: BLOCKED.** Independently re-verified the same core findings, plus: a real signup's confirmation link would error against Production's different JWT signing key even post-env-split (functional failure, not a leak); no genuine non-Owner International identity exists yet; zero-VAT/currency logic (`regionConfig.js`) is pure/TEST-safe; no TEST/PROD visible indicator exists. Conditions for PASS: fix/env-gate `emailRedirectTo`, provision a genuine International identity directly in TEST's isolated Auth, deploy Edge Functions with TEST-specific secrets, add a visible banner.

**18. Claude Lead reconciliation**: the two verdicts describe the **same** underlying gaps (zero Edge Functions, zero TEST users, the email-redirect hardcode), affecting both markets symmetrically — the disagreement is framing, not a different technical reality. **Local does not activate meaningfully "before" International** — both require the identical three prerequisites (env-split, one Edge Function, one Admin-API user), and the Admin-API path is exactly as available and exactly as safe for a fresh International identity (created inside the fully isolated `quotecode-test` project, zero collision with the Owner's real account) as for Local. The one genuine asymmetry is procedural, not technical: International TEST-identity naming carries a higher discipline bar given the project's history with the Owner's real account. EN's BLOCKED verdict and its conditions were **not overridden** — all four are folded into the execution plan (item 20) rather than dismissed.

**19. Recommended execution order** (full detail in `PROFLOW_HANDOFF.md` §18.CZ):

| Step | Objective | Mutation | Target | Risk | Prerequisite | Rollback | Verification | Owner auth required |
|---|---|---|---|---|---|---|---|---|
| A | `.env.localtest.local` + `--mode localtest` Vite wiring (port 5186 recommended) | Local file only | filesystem | LOW | none | delete the file | confirm 5186 resolves TEST's URL, 5184 unaffected | YES |
| B | Deploy `get-public-quote` to TEST + TEST-specific secrets | Edge Function deploy + `supabase secrets set` | `quotecode-test` only, target-guarded | LOW-MEDIUM | none (pairs with A) | `supabase functions delete` on TEST only | invoke against a known TEST quote ID | YES |
| C | Create 1 Local TEST user (Admin API) | TEST Auth + resulting `business_settings` row | `quotecode-test` only | LOW | A + B | delete the user on TEST | log in via browser, confirm `country='Local'`/`currency='ILS'` | YES |
| D | Create 1 International TEST user (Admin API) | same as C, market='International' | `quotecode-test` only | LOW + naming discipline | A + B | same as C | log in via browser, confirm market fields | YES |
| E | Owner visual review (Minimum Visible Milestone reached) | none | — | — | A-D | — | — | N/A (Owner's own step) |
| F+ | Full Functional items (send-quote-email, chat-ai, banner, emailRedirectTo fix, storage_path fix) | varies, each own scope | varies | varies | E | per-item | per-item | YES, each separately |

**20. Recommended next Owner decision**: **Step A only** — create `.env.localtest.local` + Vite mode wiring. Zero DB/Auth/Storage/code risk, fully reversible, prerequisite to every later step. **NOT AUTHORIZED** until Owner + ChatGPT explicitly approve it.

**21. Primary verdict**: **TEST RUNTIME ACTIVATION AUDIT: GO WITH CONDITIONS.**

**22. Confirmation no mutation**: confirmed. No application-code change, no Edge Function deployment, no Auth configuration change, no TEST user creation/modification, no Vite rewiring, no `.env` modification, no database mutation, no Storage mutation, no Production mutation, no commit, no push, no deploy, no LIVE action. The two CLI `link` commands used during target-guarded TEST baseline confirmation are non-mutating (metadata-only) and the CLI was restored to its exact pre-task Production-linked state, verified.

**23. Documentation files changed**: `PROFLOW_PROJECT_CONTEXT.md` (§24 new item 11), `PROFLOW_ARCHITECTURE.md` (§1.A extended with TEST-activation/Edge-Function-secret-pattern findings), `PROFLOW_HANDOFF.md` (§18.CZ entry appended, CURRENT RESUME STATE step-sequence extended to step (23)), `PROFLOW_TODO.md` (new dated paragraph under "Current QA / Release Track"), `PROFLOW_CHAT_HANDOFF.md` (new §10.L summary), `PROFLOW_CLAUDE_LATEST_REPORT.md` (this file, rewritten fresh).

**24. Secret/privacy scan**: `git diff` on all six changed docs, scanned for `PGPASSWORD=`, `apikey=`, `api_key=`, `service_role...=`, JWT patterns, `sk-` patterns. Only the same pre-existing, already-redacted historical incident-description line (`PGPASSWORD="<a real value>"`, a placeholder) seen in every prior scan this engagement, unrelated to this task's edits. `.env` was read this task but only key **names** were surfaced (via a `sed` redaction), never values — confirmed no value appears in any diff or in this report.

**25. Final git state (primary tree)**: `main` `HEAD == origin/main == 17ac4d3a950d96f4167f9b320c82b4798382d621`, unchanged — no commit, no push performed on `main`.

**26. Final CLI link state**: Production (`ixabnzhjeqevtbhdfswv`) = `linked:true`, `quotecode-test` (`ljfizgrdyzxddswcedwr`) = `linked:false` — verified via a fresh `supabase projects list` after the final relink, matching the exact pre-task baseline.

## Six-File Continuity Ledger

| File | Status | Reason |
|---|---|---|
| `PROFLOW_PROJECT_CONTEXT.md` | **UPDATED** | New §24 item 11 recording the hardcoded `emailRedirectTo` finding as a permanent Known Open Issue |
| `PROFLOW_ARCHITECTURE.md` | **UPDATED** | §1.A extended with TEST-activation state (0 Edge Functions, never pointed at) and the Edge Function secret-pattern split |
| `PROFLOW_HANDOFF.md` | **UPDATED** | New §18.CZ entry appended; CURRENT RESUME STATE step-sequence extended with step (23) |
| `PROFLOW_TODO.md` | **UPDATED** | New dated status paragraph appended to "Current QA / Release Track" §E, continuing the established pattern |
| `PROFLOW_CHAT_HANDOFF.md` | **UPDATED** | New §10.L summary added for ChatGPT |
| `PROFLOW_CLAUDE_LATEST_REPORT.md` | **UPDATED** | Rewritten fresh for this task |

## Final Stop

STOP after audit + documentation sync. Do not deploy Edge Functions. Do not configure Auth. Do not create TEST users. Do not modify `.env`. Do not rewire Vite. Do not fix `storage_path`. Do not implement Warranty. Do not touch Production. Wait for Owner + ChatGPT review and explicit authorization.
