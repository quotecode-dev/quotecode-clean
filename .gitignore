# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
google-indexing-key.json
.claude/

# Local secrets - never commit real keys (Stripe, Supabase, invoicing, etc.).
# See .env.example for the variable names to fill in locally.
.env
.env.local
.env.*.local

# Authorized defensive security review export (read-only source package for
# an external pentester) - never committed, local artifact only.
pentest-source-review/
pentest-source-review.zip
