# ProFlow - סיכום פיתוח, ארכיטקטורה ופיצ'רים (מעודכן לגרסה v14.1 - אוגוסט 2026)
מסמך תיעוד טכני ועסקי למערכת ProFlow לצורך רפרנס עתידי והמשך פיתוח רציף.

### 0. פרופיל הפרויקט וסביבת עבודה
* **שם הפרויקט:** ProFlow (מערכת SaaS לניהול עסק והפקת הצעות מחיר חכמות).
* **דומיין הייצור:** https://www.quotecodepro.com/
* **סטאק טכנולוגי:** React (Vite), Supabase (Auth, Database, Edge Functions), Vercel (Hosting), GitHub (Version Control).
* **סביבת פיתוח:** ענן בלבד (אין Localhost). כל השינויים מנוהלים בענן וב-GitHub תחת תגיות גיבוי מסודרות (הגרסה הנוכחית מעודכנת ל- `v14.1`).
* **ארכיטקטורה מודולרית (Modular Component Design):** פיצול קבצים גדולים לרכיבים קטנים ועצמאיים (כגון `AuthScreen`, `QuotesTab`, `SettingsTab`, `QuoteForm`) לתחזוקה קלה, קריאות קוד וביצועים מהירים.

---

### 1. מערכת דיוור (Email Integration - Supabase & Resend)
* **ענן בלבד:** ביטול מלא של פרוטוקול `mailto:` המקומי למניעת קפיצת חלונות אזהרה של Windows. השליחה מבוצעת דרך Supabase Edge Functions תחת הפונקציה `send-quote-email`.
* **אימות דומיין (Verified Domain):** השליחה מבוצעת דרך דומיין הבית (`@quotecodepro.com`) דרך ה-API של Resend, מה שמבטיח עבירות (Deliverability) מקסימלית ומונע שגיאות 403/400.
* **ניהול כתובות ותיבות:** שימוש בכתובת מרכזית (`info@quotecodepro.com`) וכינויים נלווים (`support@quotecodepro.com`) לניהול פניות הלקוחות והתמיכה.
* **תמיכה דינמית בשפות ולוגו:** הפונקציה מקבלת מה-Client פרמטרים (`isHebrew`, `logoUrl`) ומייצרת תבנית HTML רספונסיבית (RTL בעברית ללקוחות מקומיים, LTR באנגלית לבינלאומיים, והזרקת לוגו העסק).
* **מערכת חיווי סטטוס (Email Status Indicator):** נורית חיווי ויזואלית (ירוק להצלחה, אדום לכישלון) בעמודת "מייל" בטבלת הצעות המחיר (`QuotesTab`), המתעדכנת בזמן אמת.

---

### 2. אינטגרציית WhatsApp
* **שליחה ישירה וחכמה:** קישור דינמי (`wa.me`) הפותח הודעה מוכנה מראש הכוללת שם לקוח, מספר הצעה, סכום עם חיווי מע"מ, וקישור ישיר למסמך הדיגיטלי.
* **עיבוד מספרי טלפון (Normalization):** זיהוי אוטומטי של עסק ישראלי והמרת קידומות ב-CRM למניעת שגיאות API.

---

### 3. עוזר וירטואלי (AI Chat Widget)
* **מיקום וזמינות:** רכיב צ'אט (`AIChatWidget.jsx`) קבוע בצד המערכת למענה מיידי לתמיכה טכנית, עזרה בהפקת הצעות והכוונה למסלולי שדרוג.
* **לוג שאלות ותשובות:** תיעוד שאילתות משתמשים בטבלת `chat_logs` במסד הנתונים לסיווג חכם (כללי, ביטול מנוי, בקשת פיצ'ר, שאלות קשות/תקלות).

---

### 4. לוקליזציה, מטבעות, תאריכים ומיסוי (מע"מ)
* **זיהוי אוטומטי:** קריאת הגדרות דפדפן ואזור זמן לקביעת שפת ממשק ומטבע ברירת מחדל.
* **שדה תאריך מותאם אזורית (Smart Date Picker):**
    * תצוגת פורמט מדויקת המותאמת למדינה ולאזור המטבע: ישראל, לונדון וברלין (`ILS`, `GBP`, `EUR`) בפורמט התקני **`DD-MM-YYYY`**; ארה"ב (`USD`) בפורמט האמריקאי **`MM-DD-YYYY`**.
    * שילוב אייקון יוחסין ויומן מובנה (`showPicker()`) המאפשר בחירת תאריך נוחה מתוך לוח שנה דיגיטלי מבלי לפגוע בפורמט התצוגה המקומי.
* **חוקיות מע"מ (VAT Rules):**
    * 18% מע"מ מחושב ונגבה אך ורק מלקוחות מקומיים בארץ.
    * 0% מע"מ מחושב עבור לקוחות בינלאומיים.

---

### 5. הגדרות מערכת ו-CRM (`SettingsTab.jsx`)
* **פרופיל עסק:** הזנת פרטים מלאים (שם עסק, ח.פ/עוסק, אימייל, טלפון, מטבע פעיל, וכתובת מפורקת הכוללת מיקוד, מדינה ועיר).
* **ניהול מנויים:** מעקב אחר ימי ניסיון, שדרוג מסלולים (Free, Basic, Pro), ואפשרות לניהול או ביטול מנוי.

---

### 6. ניהול הרשאות (Roles) ודוחות (Super Admin)
* **מודל SaaS (Paywalls):** Free (עד 5 הצעות), Basic (עד 20 הצעות), PRO (ללא הגבלה, לוגו, וואטסאפ, מחיקה).
* **פאנל Super Admin:** ניהול משתמשים, שינוי חבילות ידני, והענקת "מנוי לכל החיים" (Lifetime).
* **דוחות כלכליים:** מרכז שליטה פיננסי הכולל גרפים (`Recharts`) ודוחות הכנסות מול הוצאות עם סינונים מתקדמים.

---

### 7. חוקי הברזל והפרוטוקול הארכיטקטוני (Iron Rules)
* **החלטות היסטוריות ועסקיות:** ניתוח מלא של היסטוריית השיחות והחלטות קודמות טכנית ועסקית לפני כל פיתוח חדש.
* **אספקת קובץ מלא בלבד:** בעת ביצוע שינויי קוד, אספקת הקובץ המלא מתחילתו ועד סופו ללא חלקי קוד.
* **פקודות Git:** צרוף פקודות גיט בתחילת או בסיום כל עדכון קוד בבלוק ייעודי.
* **הדגשות ויזואליות ואזהרות:** אזהרות יודגשו באדום בולט באמצעות HTML: `<span style="color:red;"><b>...</b></span>`.
* **הפרדת שפות קפדנית:** לוקליזציה קשורה אך ורק להגדרות העסק והמדינה.

---

<span style="color:red; fontweight:bold;">אזהרה קריטית: חובה לבצע גיבוי מלא לענן כעת!</span>

```bash
git add . ; git commit -m "v14.1-update-architecture-and-documentation" ; git push origin main --force