# ProFlow - סיכום פיתוח, ארכיטקטורה ופיצ'רים (מעודכן לגרסה v9.50 - אוגוסט 2026)
מסמך תיעוד טכני ועסקי למערכת ProFlow לצורך רפרנס עתידי והמשך פיתוח רציף.

### 0. פרופיל הפרויקט וסביבת עבודה
* **שם הפרויקט:** ProFlow (מערכת SaaS לניהול עסק והפקת הצעות מחיר חכמות).
* **דומיין הייצור:** https://www.quotecodepro.com/
* **סטאק טכנולוגי:** React (Vite), Supabase (Auth, Database, Edge Functions), Vercel (Hosting), GitHub (Version Control).
* **סביבת פיתוח:** ענן בלבד (אין Localhost). כל השינויים מנוהלים בענן וב-GitHub תחת תגיות גיבוי מסודרות (הגרסה הנוכחית מעודכנת ל- `v9.50`).
* **ארכיטקטורה מודולרית (Modular Component Design):** פיצול קבצים גדולים (כמו ה-Dashboard) לרכיבים קטנים ועצמאיים (כגון `AuthScreen`, `QuotesTab`, `SettingsTab`, `AdminUsersTab`) לתחזוקה קלה, קריאות קוד וביצועים מהירים.

### 1. מערכת דיוור (Email Integration - Supabase & Resend)
* **ענן בלבד:** ביטול מלא של פרוטוקול `mailto:` המקומי למניעת קפיצת חלונות אזהרה של Windows. השליחה מבוצעת דרך Supabase Edge Functions תחת הפונקציה `send-quote-email`.
* **אימות דומיין (Verified Domain):** השליחה מבוצעת תחת `info@quotecodepro.com` דרך ה-API של Resend, מה שמבטיח עבירות (Deliverability) מקסימלית ומונע שגיאות 403/400.
* **תמיכה דינמית בשפות ולוגו:** הפונקציה מקבלת מה-Client פרמטרים (`isHebrew`, `logoUrl`) ומייצרת תבנית HTML רספונסיבית (RTL בעברית ללקוחות מקומיים, LTR באנגלית לבינלאומיים, והזרקת לוגו העסק).
* **מערכת חיווי סטטוס (Email Status Indicator):** נורית חיווי ויזואלית (ירוק להצלחה, אדום לכישלון) בעמודת "מייל" בטבלת הצעות המחיר (`QuotesTab`), המתעדכנת בזמן אמת.
* **בדיקת תקינות מחמירה (Strict Validation):** חסימה מוחלטת (הן בצד הלקוח והן לפני הפנייה לשרת) של כתובות אימייל שגויות, פיקטיביות, או עם שגיאות הקלדה בסיומת (למשל `.comuuj`), כדי להגן על מוניטין השליחה (Sender Score).

### 2. אינטגרציית WhatsApp
* **שליחה ישירה וחכמה:** קישור דינמי (`wa.me`) הפותח הודעה מוכנה מראש הכוללת שם לקוח, מספר הצעה, סכום עם חיווי מע"מ, וקישור ישיר למסמך הדיגיטלי.
* **עיבוד מספרי טלפון (Normalization):** זיהוי אוטומטי של עסק ישראלי והמרת קידומות ב-CRM (למשל הפיכת '050' ל-'97250') למניעת שגיאות API.

### 3. עוזר וירטואלי (AI Chat Widget)
* **מיקום וזמינות:** רכיב צ'אט (`AIChatWidget.jsx`) קבוע בצד המערכת למענה מיידי לתמיכה טכנית, עזרה בהפקת הצעות והכוונה למסלולי שדרוג.

### 4. לוקליזציה, מטבעות ומיסוי (מע"מ)
* **זיהוי אוטומטי:** קריאת הגדרות דפדפן ואזור זמן (`Asia/Jerusalem`) לקביעת שפת ממשק ומטבע ברירת מחדל (`ILS`).
* **חוקיות מע"מ (VAT Rules):**
    * 18% מע"מ מחושב ונגבה אך ורק מלקוחות מקומיים בארץ (`business_settings.country === 'Local'`).
    * 0% מע"מ מחושב עבור לקוחות בינלאומיים (`business_settings.country === 'International'`).
* **סוג לקוח:** הפרדה בין לקוח עסקי לפרטי המשפיעה על תנאי התשלום, התקנון והצגת שורת המע"מ.

### 5. הגדרות מערכת ו-CRM
* **פרופיל עסק:** הזנת כתובת לוגו (URL) שנטענת אוטומטית למיילים ולהצעות (פיצ'ר סגור למנויי PRO).
* **מנגנוני הגנה:**
    * **נעילה משפטית (Lock):** חסימת עריכת הצעת מחיר שאושרה/נחתמה.
    * **הצעה חמה (Hot Quote):** זיהוי פתיחת הצעה מעל 3 פעמים ללא חתימה והקפצת התראה עליונה לבעל העסק (Dashboard Alert).

### 6. ניהול הרשאות (Roles) ודוחות (Super Admin)
* **מודל SaaS (Paywalls):** Free (עד 5 הצעות), Basic (עד 20 הצעות + עריכה/שכפול), PRO (ללא הגבלה, לוגו, וואטסאפ, מחיקה).
* **פאנל Super Admin:** ניהול משתמשים (`AdminUsersTab`), שינוי חבילות ידני, והענקת "מנוי לכל החיים" (Lifetime) המבטל תאריך תפוגה. כולל תצוגת משתמשים פעילים בזמן אמת ומשתמשים חדשים.
* **דוחות כלכליים:** מרכז שליטה פיננסי הכולל גרפים (`Recharts`) ודוחות הכנסות מול הוצאות עם סינונים מתקדמים (חודשי, חצי-שנתי, מותאם אישית).

### 7. רכיב לוגו דינמי (ProFlowLogo) ותיקון ניגודיות
* **זיהוי נתיב עמוד (Route):** התאמת צבע טקסט "Pro" באופן דינמי (כהה בדשבורד הלבן, לבן בדפי הנחיתה הכהים).

### 8. ניקוי דף הנחיתה הבינלאומי (LandingGlobal)
* הסרת סרטון חיצוני והבטחת מיתוג נקי, מדויק ובלעדי ל-ProFlow כולל פיצ'רים, FAQ ומסלולי מחירים המותאמים לשוק הגלובלי.

### 9. ניהול גיבויים ותגיות (GitHub)
* ניהול ענן מוקפד ושמירת תגיות גיבוי רציפות. חובה לגבות בסיום סבבי פיתוח תוך ציון הגרסה האחרונה. כל קוד קריטי ומסמך הארכיטקטורה נשמרים כנקודת אמת יחידה (Single Source of Truth).

### 10. אסטרטגיית סרטונים ו-YouTube
* סרטונים בפורמט אופקי (16:9). הפרדה מוחלטת: סרטון בינלאומי ציבורי (Public), סרטון מקומי בעברית מוגדר כ"לא רשום" (Unlisted) ומוטמע באתר הישראלי בלבד.

### 11. חוקי הברזל והפרוטוקול הארכיטקטוני (Iron Rules)
* **החלטות היסטוריות ועסקיות:** לפני כל מענה או הצעה לפיתוח, יש לנתח את כל היסטוריית השיחות (טכני ועסקי) ולוודא ששום שלב חדש לא סותר החלטות קודמות או בוצע כבר.
* **אספקת קובץ מלא בלבד:** בעת ביצוע שינויי קוד (React, Tailwind, Supabase וכו'), יש לספק תמיד את הקובץ המלא מתחילתו ועד סופו. לעולם אין לספק חלקי קוד או הוראות הוספה ידניות.
* **פקודות Git:** בתחילת/סיום עדכון קוד, יש לספק את פקודות ה-Git בבלוק קוד ייעודי (`git add . ; git commit -m "..." ; git push`).
* **שפה פשוטה:** הסברים שלב-אחרי-שלב, בצורה פשוטה ונגישה, ללא הנחת ידע מוקדם או תפקיד מפתח מתקדם.
* **הדגשות ויזואליות:** אזהרות או נקודות קריטיות יודגשו תמיד בצבע אדום בולט באמצעות HTML: `<span style="color:red;"><b>...</b></span>`.
* **הנחיות העתקה:** הוספת קו תחתון (Underscore) לפני ואחרי שורת ההנחיה להעתקת קבצים.
* **הפרדת שפות קפדנית (International Language Isolation):** לוקליזציה (`isHebrew`) קשורה אך ורק לדה-פקטו של השדה `business_settings.country`. משתמש בינלאומי לעולם לא ייחשף לעברית.
* **הגנת תצוגת בעל העסק (Owner View Safety):** כאשר בעל עסק צופה בהצעה משלו מתוך לוח הניהול (`PublicQuote.jsx`), תיבת החתימה ללקוח (`canvas`) מוסתרת לטובת תצוגה נקייה. החתימה מיועדת ופעילה אך ורק בלינק הציבורי של הלקוח.
* **בטיחות נתונים:** אי מתן אפשרות לעקוף הגבלות תכנותיות מצידי דפדפן (שימוש ב-Regex מוקשח בצד לקוח ואיסור על שמירת נתוני זבל במסד הנתונים).