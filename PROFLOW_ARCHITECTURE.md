# ProFlow — Current Technical & Product Architecture

**Document role**: this file describes how ProFlow is currently built and intended to behave — technical and product architecture only. It is one of **six** permanent project documents (see `PROFLOW_PROJECT_CONTEXT.md` §0.A/§0.B for the full, current, authoritative statement of this — the "four" this file previously said is superseded, not a live inconsistency):

1. **`PROFLOW_PROJECT_CONTEXT.md`** — current operational truth: rules, owner decisions, authorization state, open issues, the exact current checkpoint. **Authoritative for anything about current workflow, authorization, or what is/isn't approved.**
2. **`PROFLOW_CHAT_HANDOFF.md`** — a ChatGPT-conversation continuity snapshot: working relationship, accepted product/visual state, current resume point.
3. **`PROFLOW_ARCHITECTURE.md`** (this file) — current technical/product architecture.
4. **`PROFLOW_HANDOFF.md`** — detailed engineering history: audits, incidents, migrations, verification evidence, chronology. **Never rewritten to make old history match current architecture** — if this file and `PROFLOW_HANDOFF.md` disagree on a *current* technical claim, repository/live evidence decides, not document precedence.
5. **`PROFLOW_TODO.md`** — the authoritative living work backlog/roadmap. Current owner-approved priority comes from here, not from this file.
6. **`PROFLOW_CLAUDE_LATEST_REPORT.md`** — a report-transport bridge holding only the newest completed Claude task's Final Report.

**Reading order for a new session**: `PROFLOW_PROJECT_CONTEXT.md` → `PROFLOW_CHAT_HANDOFF.md` → this file → `PROFLOW_HANDOFF.md` → `PROFLOW_TODO.md` → `PROFLOW_CLAUDE_LATEST_REPORT.md` → locate the current checkpoint and current priority → resume. See `PROFLOW_PROJECT_CONTEXT.md`'s Mandatory Continuity Protocol (items 17, 17.A) and the Permanent Six-File Continuity Rule (§0.B) for the full rule, including the owner's magic-phrase continuity contract.

Every claim below is labeled: **LIVE VERIFIED** (confirmed via live DB/Supabase check), **REPO VERIFIED** (confirmed by directly reading current source), **OWNER-OBSERVED** (owner personally checked and reported), **DESIGN / NOT IMPLEMENTED** (approved architecture, no code exists yet), or **EXTERNAL / NOT REPO-VERIFIABLE** (operational setup outside this repository).

---

## 1. Project Identity & Stack

ProFlow — cloud-based SaaS business-management and quoting platform. Business owners manage clients, create/send price quotes (line items, discount, VAT, digital signature, WhatsApp/email delivery), track expenses/revenue, export reports. **REPO VERIFIED**: React (Vite) frontend, Supabase (Auth, Postgres/RLS, Edge Functions), Vercel hosting, GitHub version control. **Development workflow (REPO/HANDOFF VERIFIED, corrected — previously documented as "Cloud-only... no localhost dev environment used," which is no longer accurate)**: a local Vite dev server (`npm run dev`, typically `--port 5183 --strictPort`) is routinely used to live-test uncommitted working-tree changes against the live Supabase backend before they are committed; GitHub remains the sole persistent source of truth and Vercel the sole deployment target — no working-tree/localhost state is ever treated as live or deployed until committed, pushed, and built by Vercel. Modular component design (`Dashboard.jsx` composed of `AdminUsersTab.jsx`, `QuotesTab.jsx`, `SettingsTab.jsx`, `QuoteForm.jsx`, `UserDetailsModal.jsx`, `AILogs.jsx`, `AIChatWidget.jsx`, etc.).

No standalone version-number marker is maintained in this file — current project state is tracked via `PROFLOW_HANDOFF.md`'s own `HEAD == origin/main == <hash>` convention and `PROFLOW_PROJECT_CONTEXT.md`'s checkpoint, not a `vXX.X` label here.

## 1.A Supabase Environments (added 2026-08-28)

**REPO/CLI VERIFIED**: two Supabase projects exist, both in organization `smcrpeczwtpujpnhurwz`, region `eu-central-1` (Central EU/Frankfurt):

- **Production**: ref `ixabnzhjeqevtbhdfswv` (name `quotecode`), created 2026-07-27 — the real, deployed backend for the live application.
- **TEST**: ref `ljfizgrdyzxddswcedwr` (name `quotecode-test`), created 2026-08-27 — disposable/isolated, no GitHub repository connected, intended only for runtime migration/schema validation before a change is ever authorized against Production. **Updated 2026-08-30**: no longer schema-empty — now carries the Quote-Number/Attn migration chain (`20260827*`/`20260828*`) plus the **full** Phase-1 base-schema capture package (`20260830000000`-`20260830000003`, all four files including Storage), giving it the full 9-application-table Production-parity schema **plus** the `quote-files` Storage bucket and its 2 RLS policies (byte-for-byte parity with Production, verified before and after apply). See `PROFLOW_HANDOFF.md`'s Full Runtime TEST Environment Build entries (§18.CC onward) for the full history. Contains fictional test data only — never real customer data, never David Aluminum. **Updated 2026-08-30 (TEST Runtime Activation Audit, read-only)**: `quotecode-test` has **zero Edge Functions deployed** (confirmed via `supabase functions list`) and the application has never actually been pointed at it — both local Vite dev servers (5184/5186) still load the default `.env`, which points `VITE_SUPABASE_URL` at Production. No TEST Auth users exist beyond 5 DB-only `business_settings` fixture rows (no matching `auth.users`, cannot log in). A genuinely usable browser-testable TEST runtime is therefore still a future, separately-authorized activation effort — schema/Storage readiness alone does not make TEST usable. Full read-only audit, dependency map, and execution plan: `PROFLOW_CLAUDE_LATEST_REPORT.md`'s TEST Runtime Activation Audit; permanent findings also recorded in `PROFLOW_PROJECT_CONTEXT.md` §24 item 11 (hardcoded Production `emailRedirectTo`). **Updated 2026-08-30 (TEST Runtime Activation, Step A — implemented)**: port 5186 is now the dedicated TEST-pointed dev server (`npm run dev:localtest`, i.e. `vite --mode localtest --host --port 5186 --strictPort`), loading a new gitignored `.env.localtest.local` (`VITE_SUPABASE_URL=https://ljfizgrdyzxddswcedwr.supabase.co`, `VITE_PROFLOW_ENV=TEST`, `VITE_SUPABASE_ANON_KEY=` a placeholder pending the Owner pasting in TEST's real anon key) on top of the default `.env`. Port 5184 (`npm run dev`, plain, no mode) remains the Production-pointed default, byte-unchanged. `src/shared/supabase.js` now carries a fail-closed guard, active only when `import.meta.env.MODE === 'localtest'`: it requires `VITE_PROFLOW_ENV === 'TEST'`, parses the Supabase project ref out of `VITE_SUPABASE_URL`, and throws — before `createClient()` is ever called — if the ref is missing/malformed, equals Production's (`ixabnzhjeqevtbhdfswv`), or is anything other than TEST's (`ljfizgrdyzxddswcedwr`). Proven real (not just written) by temporarily pointing the TEST env at Production's ref and at a malformed URL, executing the actual served guard code, and confirming both throw correctly before restoring the correct config. Default/Production mode is untouched by this guard (code path never entered when `MODE !== 'localtest'`). No TEST Edge Function, Auth config, user, DB, or Storage state was touched by this step — see `PROFLOW_CLAUDE_LATEST_REPORT.md`'s Step A report for full detail. **Updated 2026-08-30 (Step A.1 Final Connectivity Proof)**: `.env.localtest.local`'s `VITE_SUPABASE_ANON_KEY` is **no longer a placeholder** — the Owner supplied `quotecode-test`'s real publishable key directly, installed without ever being displayed/logged. A genuine authenticated read-only REST request against TEST (`GET .../rest/v1/business_quote_sequences?select=*&limit=0`) returned `HTTP 200` with a real PostgREST `Content-Range` header and zero rows (RLS-correct for an anonymous request) — proving the TEST frontend route can now make real, successfully-authenticated Supabase calls, entirely isolated from Production. Still no TEST Edge Function deployed, no TEST Auth user exists, no DB/Storage mutation occurred — see `PROFLOW_CLAUDE_LATEST_REPORT.md`'s Step A.1 Final report for full detail.

**Edge Function secret pattern (found during the TEST Runtime Activation Audit, 2026-08-30)**: Production's 9 locally-tracked Edge Functions split across two distinct service-credential patterns — 6 read `Deno.env.get('SUPABASE_SECRET_KEYS')` (a JSON map, `['default']` extracted; the newer, migrated pattern per `PROFLOW_HANDOFF.md` §18.N/§18.P) — `get-public-quote`, `send-quote-email`, `admin-cleanup-user-quotes`, `billing-checkout-stub`, `resend-email-webhook`, `send-trial-expiration-email`; 3 still read the legacy `SUPABASE_SERVICE_ROLE_KEY` directly — `chat-ai`, `admin-delete-user`, `send-subscription-expiration-email` (this matches §18.P's own remaining-migration list). **Two Production-deployed functions have no local source tracked in this repository at all**: `clever-processor` (purpose unknown, not documented anywhere in the six continuity files) and `send-welcome-email` (previously confirmed by Agent EN not to be invoked by any signup flow). Neither is required for TEST activation; both represent undocumented drift the Owner should eventually clarify.

See `PROFLOW_PROJECT_CONTEXT.md` §17.D for the permanent TEST ≠ PRODUCTION target-guard rule governing any future use of the TEST project.

## 2. Domain / Deployment

**REPO VERIFIED / OWNER-OBSERVED, UPDATED 2026-08-31 (`PROFLOW_PROJECT_CONTEXT.md` §103, previously stale)**: canonical production domain is `https://www.quotecodepro.com/`. A host-conditional permanent redirect from `quotecode.vercel.app` was added (`e030017`, already on `origin/main`) and **mostly works** — confirmed live via both HTTP and browser checks for `/en`, `/he`, `/dashboard` (query string preserved). **One confirmed, precisely root-caused gap remains**: the literal root path `quotecode.vercel.app/` still serves the app directly (200 OK, no redirect) instead of redirecting — caused by `middleware.ts`'s root-only, non-host-aware `matcher: ['/']` intercepting the root path on every host (including the Vercel-domain host) before `vercel.json`'s host-conditional redirect rule can apply. A repo-wide grep still finds **zero references to `vercel.app` anywhere in `src/` or `supabase/`** — nothing in application code depends on that domain; the gap is routing-layer only.

**⚠️ OWNER-VERIFIED (2026-08-28, directly confirmed in the Vercel dashboard)**: this repository's Vercel project auto-deploys to Production on every push to `main` — application commits, documentation-only commits, and any other commit alike. Owner directly observed commit `ffc741d` (an application release-candidate commit) auto-deploy, and observed subsequent documentation-only commits also trigger new Production deployments. **`git push origin main` is therefore functionally equivalent to a Production frontend deployment authorization for this repository**, not a separate, lower-risk action — see `PROFLOW_PROJECT_CONTEXT.md` §17.E for the resulting permanent workflow rule (push-to-main now requires its own explicit authorization, distinct from documentation-commit authorization). This is a frontend-only deployment path — it does not touch the Supabase database, RLS, or Edge Functions, which remain on their own entirely separate, manually-triggered deployment paths (`supabase db push`, `supabase functions deploy`).

## 3. Market Architecture: Local (Israel) vs. International

### 3.1 Current product rules (LIVE VERIFIED)

| | Local / Israel | International |
|---|---|---|
| Language | Hebrew | English |
| Direction | RTL | LTR |
| Currency | ₪ (ILS) only | USD / EUR / GBP (switchable active currency) |
| VAT | 18%, automatic for local clients | 0% |
| Entry bundle | `AppLocal.jsx` | `AppGlobal.jsx` |
| Routes | `/`, `/he`, `/dashboard` (`bundleIsHebrew={true}`), `/tools`, `/he/tools`, `/terms`, `/privacy`, `/contact` (+`/he/...`) | `/`, `/en`, `/dashboard` (`bundleIsHebrew={false}`), `/tools`→`PublicToolsEn`, `/terms`, `/privacy`, `/contact` (+`/en/...`) — **no `/he` route exists in this bundle at all** |

`main.jsx` — not `App.jsx` — is the real top-level entry point; it renders `AppLocal`/`AppGlobal` directly. (`src/App.jsx` contains its own separate, unused signUp/routing logic and is **not part of the live bundle**.)

### 3.2 Pre-signup market discovery (LIVE VERIFIED — anonymous routing only)

`main.jsx`'s bundle-selection cascade, in priority order: `?lang=` query param → `/en`/`/he` path prefix → `localStorage.proflow_lang` (from a prior visit) → `proflow_geo_country` cookie (set by `middleware.ts` from Vercel's real geo header) → browser language. **This governs which landing page an anonymous visitor sees — it is explicitly never a source of truth for an existing account's market**, and is documented in-code as such.

**Known limitation, confirmed 2026-08-30 (TEST Login / Market Routing Audit, read-only)**: this cascade runs once, synchronously, before React mounts, and is never re-evaluated after login. `AppLocal.jsx`/`AppGlobal.jsx` each set `document.documentElement.lang`/`dir` in a mount-only effect (by design — never derived from `business_settings`). Dashboard's actual *content* (text/currency/VAT) is separately, robustly derived from the authenticated account's real `business_settings.country` post-login (§14.A/regionConfig.js) — but the page's `dir`/`lang` attribute itself is never re-synced to that real market. Net effect: an account can authenticate correctly while its browser remains on the "wrong" bundle, producing fully correct content trapped inside a mismatched RTL/LTR page direction — a layout defect, not a data/currency/VAT defect. `App.jsx`'s unused market-redirect logic (mentioned above) would structurally address this if it were ever wired in, but it is dead code today. **Updated 2026-08-30 (Item 25 Automatic Post-Login Market Routing Fix task) — FIXED IN WORKING TREE, NOT YET OWNER-VERIFIED WITH REAL TEST USERS.** `src/utils/regionConfig.js`'s new `getMarketRoutingCorrection(...)` — a pure, exported, unit-tested function — is the single shared decision (identical for both markets, no per-bundle duplicate) for whether the currently-mounted bundle needs correcting; `src/pages/Dashboard.jsx` gained exactly one new `useEffect` that calls it and, when it returns a target, performs one `window.location.href = '/dashboard?lang=he'` (or `?lang=en`) — the existing `?lang=` mechanism already at the top of `main.jsx`'s own cascade, not a new route. The function only acts once `business_settings` has genuinely loaded (`settingId` non-null) — never on the initial cache/guess — and is provably loop-free (post-redirect, `isHebrew` and `bundleIsHebrew` match, so it returns `null` on the next check). `App.jsx`'s dead logic was deliberately not revived (it reads market from `user_metadata`, not `business_settings.country`, and is otherwise architecturally stale). Not committed/pushed/deployed. **Updated 2026-08-30 (Item 25 End-to-End TEST User Verification task)**: a real browser login (via an isolated headless Chrome driven over raw CDP, since Browser Harness's daemon remained unavailable) succeeded for both of the Owner's new TEST Auth users, with sessions correctly persisting across refresh — but the actual correction redirect could not be observed live, because neither TEST account has ever been assigned a real `business_settings.country` (zero rows, no `signup_market`, and local TEST has no working geo-detection fallback) — both fall into the pre-existing `needsRegionChoice` fail-safe, which this fix correctly never overrides with a guess. Not an Item 25 defect. Recorded as `PROFLOW_TODO.md` item 25 and `PROFLOW_PROJECT_CONTEXT.md` §24 item 12; full detail in `PROFLOW_HANDOFF.md` §18.DE/§18.DF. **Updated 2026-08-30 (Item 25 E2E Unblock + Final Verification task) — E2E LIVE VERIFIED, PASS FOR BOTH MARKETS.** The Owner authorized completing the app's own existing region-selection UI once per TEST account (a real click on the real rendered screen, no SQL) — the application's own logic created each `business_settings` row (`Local`/`ILS`, `International`/`USD`). Separately, this task found that a bare `signInWithPassword()` API call does not reliably trigger `Dashboard.jsx`'s own post-login `loadData()` sequence (confirmed via live network-request tracing showing zero REST queries fired that way) — driving the actual rendered login form (real DOM inputs, real submit) fires the complete, correct sequence every time; verification tooling was corrected to use the real form. With both accounts genuinely initialized and real-form-signed-in, Item 25's correction was observed live: Local, starting on the mismatched International bundle, corrected to `dir=rtl`/`lang=he` with `₪` displayed and no `$`; International, starting on the mismatched Local bundle, corrected to `dir=ltr`/`lang=en` with `$` displayed and no `₪` — both stable across two refreshes, no redirect loop. Both agents independently reran every scenario and reproduced identical, symmetric results. Full detail in `PROFLOW_HANDOFF.md` §18.DG.

### 3.3 Post-signup account market (LIVE VERIFIED — current, incomplete state)

Once `business_settings` exists for a user, `business_settings.country` is the account's market, set once at profile creation and never re-derived on subsequent logins (`fetchSettings()`'s existing-row branch never touches `country`). **This part is solid and correct today.**

**The gap (LIVE-REPRODUCED, see §6 below)**: the mechanism that determines `country` at the moment of *initial* profile creation currently depends on a fresh geo lookup or explicit user choice made at whatever moment the user's browser first reaches `Dashboard.jsx` with a session — which is not guaranteed to be the same moment, device, or location as the original signup. This has been reproduced as a live defect (§6).

### 3.4 Approved design — NOT YET IMPLEMENTED

**Status: DESIGN / GO WITH CONDITIONS. No code exists for this yet.** Three-tier authority for account market:

- **Tier 1**: `business_settings.country` — permanent authority once the row exists (already true today, §3.3).
- **Tier 2**: `auth.user_metadata.signup_market` — would become authoritative *only* for the one-time creation of a genuinely missing profile, captured at the exact moment `signUp()` is called from whichever bundle the user was actually using.
- **Tier 3**: fresh geo lookup / explicit user region-choice screen (today's existing mechanism) — demoted to a fallback used only when Tier 2 metadata is unavailable (e.g. a pre-fix legacy account).

**Do not treat Tier 2/3 as live behavior** — see §6 for the full audit and current implementation status.

## 4. Currency & VAT Architecture

**LIVE VERIFIED** (`regionConfig.js`): currency and VAT rate are derived deterministically from `business_settings.country` — Local → `ILS`/18%, International → `USD`/`EUR`/`GBP`/0% — never from UI language, never independently settable. An International account's *active* currency can be changed at any time via business settings; this is intentional product behavior and must not be "fixed" or removed.

## 5. Quote Currency / Historical Quote Preservation

**LIVE VERIFIED, owner-confirmed, must be preserved exactly**:
- Each quote **permanently freezes the currency it was created in** — changing the account's active currency only affects quotes created *after* the change; existing quotes are never retroactively rewritten.
- One International account can therefore legitimately have quote history spanning multiple currencies simultaneously. **This is correct behavior, not a leak.**
- `handleEditClick` (`Dashboard.jsx`) preserves a quote's original `currency`/`tax_rate` on save, never recomputing from the account's current region.
- **Important nuance**: a historical quote showing ₪ inside an International account is not automatically evidence of a current bug. A known example was created in ILS and is now signed/approved — the application's quote-immutability system (approved/paid/signed quotes are fully locked against edits, enforced independently at both the UI layer and via database triggers) means its currency cannot currently be changed through the normal application flow, regardless of any policy about historical data. **Future work must distinguish an actual currently-occurring currency leak from valid, locked historical data.**

## 6. Auth / Signup / Email Confirmation Architecture

### 6.1 Live-reproduced defect (LIVE VERIFIED this engagement)

A fresh International TEST signup was performed through the real signup UI to diagnose a suspected defect:

Auth signup succeeded → no active session immediately after (email confirmation is enabled) → English confirmation email received → owner clicked verify → confirmation succeeded (`auth.users.email_confirmed_at` populated, OWNER-OBSERVED) → **confirmation redirect landed on the Hebrew/Israel root landing page, not an International destination** → `business_settings` check: **zero rows** for this account.

This reproduction case is **deliberately preserved** — an Auth user that exists and is confirmed, with no `business_settings` row — and must not be repaired without separate explicit authorization.

### 6.2 Root cause (REPO VERIFIED)

The one shared `signUp()` call (`Dashboard.jsx`, used identically by both markets — this is **not** an International-only defect, Local signups have the exact same gap) is invoked with **no `options` object at all** — no `emailRedirectTo`, no metadata. Supabase therefore falls back to the project's Site URL, landing the confirmation click on the bare domain root. Neither `LandingLocal.jsx` nor `LandingGlobal.jsx` has any session-awareness (no `getSession()` call). The only code that creates `business_settings` (`fetchSettings()`/`createNewBusinessSettings()`, both inside `Dashboard.jsx`) only runs when `Dashboard.jsx` mounts — which the confirmation redirect currently never triggers. Separately, `main.jsx`'s pre-signup cascade (§3.2) then re-resolves market fresh at that bare-root URL, independent of the account's actual signup market — the direct mechanism behind the Hebrew-landing-page redirect.

### 6.3 Current live Supabase Auth URL Configuration (OWNER-OBSERVED)

- **Site URL**: `https://www.quotecodepro.com` (unchanged)
- **Redirect URLs**: `https://quotecode.vercel.app/`, `https://www.quotecodepro.com`, and `https://www.quotecodepro.com/dashboard` (the last one manually added by the project owner during this diagnosis; nothing removed). Whether the bare-origin entry already covered `/dashboard` before this addition is **UNKNOWN** — not verifiable from this environment.

### 6.4 Approved fix — DESIGN / NOT YET IMPLEMENTED

Add `options: { emailRedirectTo: window.location.origin + '/dashboard', data: { signup_market: isHebrew ? 'Local' : 'International' } }` to the one `signUp()` call; have `fetchSettings()`'s missing-profile branch prefer `session.user.user_metadata?.signup_market` before falling back to the existing fresh-geo lookup. **One file (`Dashboard.jsx`), two edits. No other file, database object, RLS policy, or Edge Function requires change.** `user_metadata` is user-writable (unlike `app_metadata`) — the design consumes it **only once**, at initial profile creation, and never re-reads it once a profile exists, so a user rewriting their own metadata later cannot migrate an existing account's market. **Status: GO WITH CONDITIONS. Not implemented. Requires separate explicit owner authorization before any code change.**

## 7. `business_settings`

**LIVE VERIFIED**: `role` column is `text`, nullable, default `'user'`, no CHECK constraint, no ENUM (`'admin'` is already schema-valid without migration). Sole INSERT authority: `createNewBusinessSettings()`. `UNIQUE(user_id)` constraint — **DOCUMENTED** (not re-checked via live catalog access this engagement). Default payload at creation: `role: 'user'`, `plan: 'pro'`, `trial_ends_at: now()+14d`. Existing-row branch of `fetchSettings()` never touches `trial_ends_at` or `country` on subsequent logins.

## 8. RLS / Security Architecture

### 8.1 `business_settings` (LIVE VERIFIED)

`authenticated` has INSERT+SELECT on the `role` column but **no UPDATE** — no client, including a `super_admin` acting through the normal app, can self-write `role`. 7 RLS policies exist: ownership (ALL), two restrictive INSERT policies (force `role='user'`; force safe free/legitimate-trial plan+trial shape), `super_admin` SELECT/UPDATE via `public.is_super_admin()`, and two general owner policies. `public.is_super_admin()`: `SECURITY DEFINER`, `STABLE`, non-recursive (built specifically to avoid a `42P17` self-referential recursion incident that was hit and fixed once during its own construction), EXECUTE granted to `authenticated`/`service_role` only, `anon` explicitly revoked.

### 8.2 `chat_logs` (LIVE VERIFIED — fixed this engagement)

**Prior state**: RLS was disabled, zero policies existed, `anon`/`authenticated`/`service_role` all held full table privileges. **Live-confirmed exploit**: an ordinary authenticated non-admin TEST account performed a minimal direct Data API SELECT and received `HTTP 200` with rows — the `/ai-logs` UI gate was the *only* thing standing in the way.

**Current state (fixed)**: RLS enabled; exactly one policy, `"Super admins can view all chat logs"`, `FOR SELECT TO authenticated USING (public.is_super_admin())`; `anon` has no table privileges at all; `authenticated` reduced to SELECT only; `service_role` unchanged (the sole writer, `chat-ai` Edge Function, uses `service_role` and is unaffected by RLS by design). Post-fix, the same TEST account's identical direct SELECT returned `HTTP 200` with `[]` — zero rows. **What was not re-tested**: a second TEST account's post-fix access (that account's login was separately broken and unrelated to this fix), and a full live re-run of every four-context AI Chat functional flow (their non-interaction with this RLS change is an architectural conclusion, not a fresh end-to-end test).

## 9. Role Model: `user` / `admin` / `super_admin`

Authorization roles are **completely separate** from subscription plans (§16) — `role='admin'` must never imply or grant a paid-plan entitlement.

**LIVE VERIFIED, confirmed favorable property**: every existing `role === 'super_admin'` check in this codebase (roughly 9 call sites: 4 frontend, 5 backend/Edge-Function) is an **exact string-equality check**, never `!== 'user'` or a set-membership test. Introducing `role='admin'` with zero code changes therefore grants that account no capability beyond a plain `user` — confirmed, not assumed.

**Current DB-layer preparation (LIVE VERIFIED, owner-executed)**: `public.is_admin()` created — exact structural mirror of `is_super_admin()`, checks `role = 'admin'` exactly, same non-recursive/EXECUTE-ACL discipline. One new additive `business_settings` SELECT policy: `"Admins can view all business settings"`, `FOR SELECT TO authenticated USING (public.is_admin())` — does not touch any of the 7 pre-existing policies.

**Application-layer state: NOT STARTED.** No `isAdmin`/`isAdminOrAbove` distinction exists in frontend code. No account with `role='admin'` exists. **Corrected** (an earlier claim here that these keys were absent was stale): `PROFLOW_TEST_ADMIN_EMAIL`/`PASSWORD` keys **are present** in `.env`, but the configured email does **not** currently exist in Supabase Auth (owner-verified via Authentication → Users) — the credential set is therefore not currently usable, and the stored password is not live-verified. Provisioning remains blocked pending either creating that Auth user or supplying new credential values (never invented by an agent). See `PROFLOW_PROJECT_CONTEXT.md` §14/§22/§24 for the full corrected detail — Super Admin authorization itself is governed exclusively by `business_settings.role = 'super_admin'`, never by this email.

**Approved ADMIN V1 capability model (design only)**: read-only — Admin area access, users list, platform KPIs, search/filter, permitted user/business details. Explicitly denied: delete user, reset data, extend trial, Lifetime, plan/subscription changes, role changes, admin creation/removal, diagnostic/test-email actions, destructive Edge Functions, **and AI Support Logs / `chat_logs` access — explicitly deferred, never automatically granted merely because a broader Admin area becomes visible.**

`admin-delete-user`'s target-protection guard refuses only `role === 'super_admin'` targets — an `admin`-role target would currently fall through as deletable by a `super_admin` caller, matching the intended hierarchy (super_admin may delete admin; admin cannot delete anyone in V1, since its caller-gate check on every destructive Edge Function already requires exact `role === 'super_admin'`).

## 10. Admin UI — Requirements & Status

**The final Admin/Super Admin UI visual design is NOT yet approved.** The current dark/neon UI must not be treated as final (a light-theme redesign was separately scoped, not implemented). **Firm, standing requirement regardless of final design**: the users-management table/list must begin with a human-readable user/business **name** as the primary identity — **not email**. Email is secondary information only. **A working reference for this already exists in the same file**: `AdminUsersTab.jsx`'s own mobile card layout already shows business name as the bold primary line with email as a smaller secondary line beneath it — only the desktop table's column order (`Email` is currently the first, 21%-width, sortable column) hasn't caught up to that same file's own mobile pattern.

**Maximum Admin Audit COMPLETE, `PROFLOW_PROJECT_CONTEXT.md` §94/§94.1 (2026-08-31)** — full 24-section read-only audit, no implementation. Durable architecture facts from it: the users table's `Role` column is currently **structurally zero-information** (the table already excludes every row that isn't `role='user'` before rendering, so it can never show a differing icon) — a REMOVE candidate, not a keep-and-improve one. **The customer Dashboard shows no plan/tier badge anywhere today** — Admin's badge problem is 3 independent hardcoded implementations vs. Dashboard's zero, not a disagreement between two existing ones. **Admin has zero automated test coverage** — every currently-correct behavior (sort, search, mobile accordion, re-auth flows) is protected only by manual verification. **No `admin_audit_log` exists** — destructive/entitlement-granting actions leave no structured, attributable trail beyond raw infra logs. A full Admin Action Safety Matrix surfaced two asymmetries: Lifetime *grant* has a confirm-modal step, Lifetime *revoke* does not (instant); "Extend Trial 14d" has no confirmed action-specific server-side re-verification, unlike every other mutating admin action. Recommended single next step (not yet authorized): build the canonical resolver + plan catalog and repoint Admin's plan/Lifetime derivation at it, before any visual redesign — retires the one confirmed-live bug found across two separate audits without touching anything not yet decided.

**Surface inventory and visual-consistency finding, `PROFLOW_PROJECT_CONTEXT.md` §94 (2026-08-31, Maximum Admin Audit)**: the "current dark/neon UI" framing above is not fully accurate as a single description — **four independent, uncoordinated visual systems already exist** across the four Admin-adjacent components (`AdminUsersTab.jsx`, `UserDetailsModal.jsx`, `LifetimeConfirmModal.jsx`, `AILogs.jsx`). `AdminUsersTab.jsx` already correctly imports the shared **`LIGHT`** theme tokens; `UserDetailsModal.jsx` imports the shared **dark** `NEON` tokens (the one genuine dark holdout, reached by clicking a row in the already-light table); `LifetimeConfirmModal.jsx` and `AILogs.jsx` both use independent hardcoded hex palettes that match neither shared theme nor each other. Future redesign work should be understood as **consolidation onto one shared token system**, not a from-scratch dark-to-light rewrite — 3 of 4 surfaces are already light-ish today.

**Other Admin gaps confirmed this same audit, not yet fixed**: `AILogs.jsx` is hardcoded Hebrew/RTL-only with no English branch at all — the only Admin surface without HE/EN parity; a genuinely open product decision (should Super Admin be bilingual, or is one operational locale acceptable) is not yet resolved. Quote usage/limits (`monthlyQuotesCount`/`planLimit`, computed in the customer Dashboard) are displayed nowhere in Admin. `business_settings.currency` exists but is never read or shown by Admin. No "account enabled/disabled" concept exists anywhere in the schema — not an Admin UI gap, a genuine absence.

**Admin security re-verified, PASS/UNCHANGED**: tab visibility (`isSuperAdmin`) is client-side convenience only; every actual mutation (Lifetime toggle, trial extend, quote-data reset via `admin-cleanup-user-quotes`, full account deletion via `admin-delete-user`, the email-diagnostics test-send) independently re-verifies `super_admin` server-side from the caller's own JWT. Both destructive Edge Functions additionally require the acting admin to re-enter their own password (real step-up re-auth) before executing; `admin-delete-user` further blocks deleting any `role='super_admin'` row server-side; `admin-cleanup-user-quotes` performs a genuine post-delete verification read rather than trusting an error-free response. `AILogs.jsx`'s own redirect-on-mount guard plus the independent, previously-hardened RLS restriction on `chat_logs` remain intact, not reopened. These re-auth and verified-deletion patterns are recommended **KEEP/LOCK** candidates for any future Admin rebuild, not to be replaced merely for architectural tidiness.

## 11. AI Chat / `chat_logs` Architecture

**REPO VERIFIED**: `AIChatWidget.jsx` (shown on landing pages, contact page, and the authenticated dashboard) → `chat-ai` Edge Function (calls OpenAI `gpt-4o-mini`) → logs every exchange to `chat_logs` via `service_role` (legacy `SUPABASE_SERVICE_ROLE_KEY` — migration to the modern `SUPABASE_SECRET_KEYS` pattern remains a separate, open, unstarted track). Classification categories: `GENERAL`, `CANCELLATION`, `FEATURE_REQUEST`, `HARD_QUESTION` (keyword-based, market-isolated pricing block per §3). `AILogs.jsx` (`/ai-logs`) remains `super_admin`-only, now backed by real RLS (§8.2), not just a UI redirect. **A future restricted `admin` role does not automatically gain AI Support Logs access — this must be a separate, explicit, future decision, never a side effect of broader Admin-area access.**

## 12. Email Architecture

**REPO VERIFIED**: `send-quote-email` Edge Function sends via Resend's API, with dynamic Hebrew/English HTML templates driven by an `isHebrew` parameter, RTL/LTR-aware layout, and business-logo injection (`logoUrl`). An email-status indicator (green/red) exists in `QuotesTab.jsx`, updated via the `resend-email-webhook` function on bounce. **Open item**: `send-quote-email` still uses the legacy `SUPABASE_SERVICE_ROLE_KEY` — a tracked, unstarted credential-migration item.

**EXTERNAL / NOT REPO-VERIFIABLE**: sender domain verification with Resend, and the Namecheap-hosted `info@`/`support@quotecodepro.com` mailboxes with POP3 sync into Gmail — this is operational infrastructure outside the repository and cannot be confirmed by reading source code.

## 13. WhatsApp Integration

**REPO VERIFIED (existence)**: WhatsApp-related code (dynamic `wa.me` links, phone-number normalization) is present across `SettingsTab.jsx`, `QuoteForm.jsx`, `PublicQuote.jsx`, and other components. The exact current behavior of Israeli-business-specific phone normalization was not re-verified line-by-line in the most recent audit — treat as **DOCUMENTED**, re-confirm before relying on exact behavior in future work.

## 14. Public Quote / Storage Architecture

**REPO VERIFIED**: public quote pages are served via `SmartPublicQuote`/`PublicQuote`/`PublicQuoteEn`, resolved by ID through the `get-public-quote` Edge Function (fully anonymous — no role/auth check of any kind found in that function). Quote file attachments (`quote_attachments` table) reference objects in a Supabase Storage bucket (`quote-files`, paths prefixed by `user_id`). **Known, documented, still-open gaps** (not fixed, not to be fixed opportunistically): no owner DELETE/UPDATE policy currently exists on `storage.objects` for this bucket at all (a live test confirmed `403` on a normal authenticated delete attempt); no code path anywhere (including the user-deletion Edge Functions) ever deletes the underlying Storage file objects — only the database metadata row is removed, meaning deleted accounts likely leave orphaned Storage files behind if they ever uploaded attachments.

## 14.A Quote Number Architecture (Business/Display Identifier)

**LIVE VERIFIED (2026-08-28, direct schema/RLS/trigger introspection)**: `quotes.quote_number` already exists live as `integer NOT NULL DEFAULT nextval('quotes_quote_number_seq'::regclass)` — a single **global** sequence shared across every business (not per-business, not this repository's own design). No unique constraint, no allocation RPC, no quote_number-specific immutability trigger exists live. `quotes.id` (UUID) remains, and will always remain, the sole technical/routing/security identifier — `quote_number` is purely a customer-facing display value, never used for authorization or routing.

**Target architecture (DESIGN, local package prepared, NOT applied live)**: per-business allocation via a dedicated `business_quote_sequences` counter table (`user_id` PK, RLS default-deny, one super-admin read policy) plus a `SECURITY DEFINER` `allocate_quote_number(uuid)` function as the sole write path (atomic `INSERT ... ON CONFLICT ... DO UPDATE ... RETURNING`, re-validates `auth.uid()`). First managed allocation per business = `100700`, displayed as `A100700`. A `UNIQUE (user_id, quote_number)` index (built `CONCURRENTLY`) and a dedicated immutability trigger (blocks any post-assignment change, narrow `service_role` bypass reserved for forward-fix) complete the target state. See `supabase/migrations/20260827000000`–`20260827000003` and `supabase/quote_number_counter_init.sql`.

**Historical Number Preservation (permanent design rule)**: quotes numbered under the pre-transition global sequence (e.g. `A11`, `A56`, `A90`) are never renumbered, reinterpreted, or reassigned once the per-business scheme goes live — they remain permanent historical identifiers. Only quotes created after the transition receive numbers from the new per-business `A100700+` sequence. It is intentional and expected for an older quote to show `A90` while a newer one from the same business shows `A100700` — no fake continuity is manufactured.

**Display**: canonical formatting lives in `src/utils/quoteNumber.js` (`formatQuoteNumber`/`formatQuoteFallback`) — market-neutral, consumed identically by both `PublicQuote.jsx` (Hebrew, "מספר הצעה") and `PublicQuoteEn.jsx`/`PublicQuoteHeader.jsx` (English, "Quote Number"); only the label text is market-conditional, never the underlying value or allocation logic. Full HE/EN cross-surface status: `PROFLOW_TODO.md` item 17.

**Known live gap (flagged, not yet fixed)**: because no quote_number-specific immutability trigger exists live yet, an authenticated owner can currently change their own quote's `quote_number` via a plain `UPDATE`, for any quote not yet approved/paid/signed. Closed by the prepared (not-yet-applied) `20260827000002` migration.

**Deployment desync (Production only, still open)**: local `get-public-quote`/`send-quote-email` Edge Function source already selects `quote_number`; the currently-deployed Production versions of both (last deployed 2026-08-25, per `supabase functions list`) predate that change and do not return it — this is why Dashboard can already show a real number (e.g. "A90") while the same quote's own Public Quote page and emailed subject line still show the fallback hash on Production, today. Not deployed to Production by any task to date; a coordinated future Production release is required (`PROFLOW_TODO.md` item 17).

**TEST: target architecture now fully live and end-to-end verified (2026-08-30, TEST Implementation + Coordinated TEST Verification task)**. The full migration package (`20260827000000`-`20260827000003` + `202608270000015`) is applied on `quotecode-test`; `get-public-quote` and `send-quote-email` are both deployed there (TEST only, never Production); the one remaining application-code gap (`QuoteForm.jsx`'s internal editing header showing a raw UUID instead of the real number) was closed via a new `editingQuoteNumber` prop threaded from `Dashboard.jsx`'s existing `editingOriginalQuote` lookup, reusing the canonical `formatQuoteFallback` formatter (no new formatter introduced). Real browser-driven verification (real login, real "New Quote"/"Duplicate"/"Edit"/"Delete" clicks, real form submits) proved, for both Local and International TEST accounts independently: correct per-business sequential allocation (International's first-ever quote landed at exactly `A100700` despite Local's counter already being well past `A100710` — the definitive cross-business isolation proof), duplicate-gets-a-fresh-number-while-the-original-is-unchanged, edit-never-changes-`quote_number`, delete-then-create-never-reuses-the-deleted-number, the `QuoteForm.jsx` header showing the real number, and the Public Quote page showing the real number with no fallback. Both Agent HE and Agent EN independently reproduced these results. **One TEST-environment-only gap found**: email sending fails on TEST because the `RESEND_API_KEY` secret was never configured there (Production has one) — not a numbering defect, the email function's local source was already confirmed to compute the quote-number-bearing subject correctly before the failing Resend call. See `PROFLOW_TODO.md` item 17 for full detail and the required remaining Production-side gates.

## 14.B Warranty Architecture (Item 23)

**TEST: fully live and end-to-end verified (2026-08-30, TEST Acceptance Readiness Package 1 task).** Mirrors the pre-existing `default_terms`/`terms` snapshot pattern exactly, deliberately not inventing a new architecture: `business_settings.default_warranty` (business-level default, editable any time in Settings) and `quotes.warranty` (per-quote snapshot, copied from the business default at quote-creation time only). New TEST-only migration `20260830000004_add_warranty_fields.sql`. **Immutability**: no new trigger exists or is needed — `guard_quote_immutability()` (§ see the base-functions capture migration) copies the entire `NEW` row into a `public.quotes`-typed variable and compares it whole-row against `OLD` once a quote is locked; any column added to `quotes` is automatically covered. **Grants**: `quotes` has a table-wide grant for `authenticated`, so `warranty` needed no grant change there; `business_settings` uses deliberate column-level least-privilege grants (not table-wide) — `default_warranty` was explicitly added to both the `authenticated` `INSERT`/`UPDATE` column-privilege lists (the same gap class a prior Phase-1 review already caught once on this same table). **Duplicate semantics, live-verified both markets**: duplicating a quote copies the *original quote's own* warranty snapshot, not the business's current default — the same rule already governing every other duplicated editable field. **Display**: `PublicQuote.jsx`/`PublicQuoteEn.jsx` each render a dedicated "אחריות:"/"Warranty:" section from `quote.warranty` only (never re-fetching current Business Settings), omitted entirely when empty/null. `get-public-quote` selects and returns `warranty`, redeployed to TEST. **Known, deliberately out-of-scope gap**: `send-quote-email`'s template doesn't include Terms today either (confirmed absent), so Warranty was not added to the email body — would require new email-layout design, not a minimal symmetrical change. **Not applied to Production.**

## 14.C Structured Quote Items / Industry / Measurement / Pricing Engine (Future, P1 Full-LIVE Readiness Gate — Item 30)

**Status: 🔴 FUTURE / NOT DESIGNED / NOT IMPLEMENTED.** Unlike §14.A (Quote Number) and §14.B (Warranty), which already have live or TEST-verified implementations, this is a **documented future architecture requirement only** — no design work is authorized by this section. Full detail lives at `PROFLOW_TODO.md` item 30 (with sub-items 30.A–30.D); `PROFLOW_PROJECT_CONTEXT.md` §95 records the fuller reconciliation history. This section exists so the principle has a real architectural home, not just a TODO entry, after a prior gap where neither this file nor `PROJECT_CONTEXT.md` meaningfully surfaced it.

**Priority (declared 2026-08-31)**: the Owner has designated this architecture — not every future formula, the underlying architecture itself — as **P1, a Full-LIVE readiness gate**. The distinction matters: the override hierarchy, mixed-pricing capability, snapshot discipline, and extensibility model must exist before LIVE; specific additional pricing/measurement *methods* may be designed and added incrementally after LIVE, provided the P1 foundation was built to accept them without a breaking rewrite.

**Core principle**: industry/business-type supplies **presets** (recommended defaults — terminology, fields, measurement/pricing methods) but must never hardcode or permanently lock how a quote item is calculated. Override hierarchy: `Industry/Business preset → Business defaults → Catalog/Item defaults → Quote-item pricing method → Explicit user override` — each level defaults the next; the item itself always has final say. A single quote must architecturally support **mixed pricing methods across its items** (fixed price, quantity×unit, time×rate, length×rate, area×rate, weight×rate, and future extensible methods) — not one calculation model per business.

**Extensibility constraint**: must not be implemented as a growing `if (businessType === X)`-style branch chain — business type selects presets, calculation capability comes from an extensible model every type shares, mirroring the same anti-pattern discipline already proposed for the plan/tier catalog (§16 below / `PROFLOW_PROJECT_CONTEXT.md` §92/§94.1) — a new pricing method or industry preset becomes a config/catalog entry, never a new code branch.

**Historical quote integrity**: every structured item's original entered value, unit, pricing method, rate, quantity, calculated value, currency, and tax context must be snapshotted at save time, exactly as entered. A later change to business/catalog/industry defaults must never rewrite an already-saved quote's items; unit conversions require an explicit, auditable conversion architecture, never an implicit display-time rewrite (e.g. `36 in` must never silently become `91.44 cm` on an existing quote). This mirrors the immutability discipline already established for Quote Number (§14.A) and Warranty (§14.B) — same pattern, not a new one.

**Market separation**: measurement units, pricing method, industry/business-type, currency, and language are five independent dimensions and must never be silently coupled — an International/English business must be able to use Metric, a Local/Hebrew business must be able to use Imperial, with no assumption in either direction. Applies the same discipline as the permanent Local/International market-separation rule (§3) to this future engine specifically.

**Admin V2 compatibility**: reviewed against the completed Maximum Admin Audit (§10 above) — no conflict. Admin V2's proposed canonical resolver (plan/tier/trial/subscription entitlement) is a separate concern from this quote-pricing engine; neither blocks the other. What Admin would eventually expose about this engine (business preset, per-item calculation method on a given quote, historical snapshot data for support) is recorded as compatibility awareness only at `PROFLOW_TODO.md` §30.D — not scoped, not designed.

## 15. SEO / Routing

**REPO VERIFIED (current, re-read directly)**: `public/sitemap.xml` currently lists 11 URLs — `/`, `/he`, `/en`, `/he/tools`, `/en/tools`, `/he/contact`, `/en/contact`, `/he/privacy`, `/en/privacy`, `/he/terms`, `/en/terms` — with `hreflang` alternates. `public/robots.txt` has **no `Disallow` rules at all**; private/system routes (`/dashboard`, `/ai-logs`) are simply absent from its `Allow` list, and are actually noindexed via `X-Robots-Tag` HTTP headers configured in `vercel.json` — a deliberate design choice (blocking via `robots.txt` was tried and reverted specifically so crawlers could still fetch and see the noindex directive), not a `robots.txt` Disallow mechanism.

## 16. Trial / Plans / Billing

**LIVE VERIFIED (paywall tiers, matches current `AdminUsersTab.jsx` quote-limit logic)**: Free (5 quotes/month), Basic (20 quotes/month), Pro (unlimited, logo, WhatsApp, deletion). New signups get a 14-day full-Pro trial (§7). `handleExtendTrial14Days` deliberately refuses to extend a trial that still has time remaining — **owner-confirmed intended behavior, not a bug**.

**RESOLVED 2026-08-31** (was: "Open, separately tracked... a TEST account's trial was observed resetting toward a fresh ~14-day window between sessions"): confirmed, via a read-only own-row query against a real TEST account (`PROFLOW_TEST_LOCAL`, zero mutation), that **no reset/extension occurs** — `trial_ends_at` is exactly `created_at + 14.0000` days, stable and untouched. The "still shows 14 days" observation is a pure **display-formula discrepancy**: Dashboard's `Math.ceil()` rounding (`src/utils/planEntitlements.js:42`) inflates the real ~13.1 days remaining up to `14`, while the legacy Admin's `Math.floor()`+hours formula (`AdminUsersTab.jsx:276`) correctly shows `13 days, 2 hours` for the identical timestamp. Does not affect entitlement correctness (`isTrialExpired`/`isExpiringSoon` gating is unaffected by the ceiling). A shared display-formatting helper (both surfaces reading the same calculation) is proposed but not implemented — see `PROFLOW_PROJECT_CONTEXT.md` §93.

**No real billing/payment infrastructure exists.** `billing-checkout-stub` is pure scaffolding (no real Stripe call, no table writes, `checkoutUrl` always `null`). There is currently **no authoritative paid-subscriber source of truth** in the schema — `plan='pro'`/`'basic'` alone never proves actual payment, since every new trial signup also gets `plan='pro'` by default. Do not assume billing is implemented merely because plan tiers exist in the UI. `plan='basic'` is additionally **structurally unreachable at the database level** — two stacked RLS RESTRICTIVE INSERT policies confine signup to exactly `free/null` or `pro/+14d±2h`, and a `BEFORE UPDATE` trigger (`guard_business_settings_plan_trial`) rejects any other write except by `super_admin`.

**Owner product decisions, CLOSED 2026-08-31 (full detail `PROFLOW_PROJECT_CONTEXT.md` §91/§92)**: BASIC is a real sellable paid plan (₪0/₪49/₪99, unchanged); upgrade takes effect immediately on confirmed payment; downgrade preserves the already-paid tier's entitlement until `current_period_end`; cancellation is `CANCEL_AT_PERIOD_END`, never an immediate downgrade; Active Trial is a distinct lifecycle state from paid PRO (future badge: `TRIAL`, not `PRO`, even though current entitlement is identical). **Canonical model — Phase 1 (plan/tier + trial axes) IMPLEMENTED LOCALLY 2026-08-31, `PROFLOW_PROJECT_CONTEXT.md` §97, not committed**: `src/utils/planCatalog.js` (stable plan IDs, HE/EN labels, badge metadata, entitlement limits — free/basic/pro only, TRIAL is a display-state overlay, not a catalog entry) + `src/utils/accountEntitlement.js`'s `resolveAccountEntitlement()`, composing the existing, unchanged `computeEffectivePlan()`. The subscription-status axis (doesn't exist yet) and a separate versionable price catalog remain future work, unchanged in status.

**Known live bug, FIXED and LIVE-VERIFIED 2026-08-31, committed locally not pushed (`PROFLOW_PROJECT_CONTEXT.md` §97/§98)**: `UserDetailsModal.jsx`/`AdminUsersTab.jsx` previously derived "Lifetime" from `trial_ends_at IS NULL` alone, bypassing `computeEffectivePlan()` — a self-cancelled FREE user displayed as "PRO (Lifetime)" in Admin while correctly showing FREE on their own Dashboard. Both files now call `resolveAccountEntitlement()`, whose `isLifetime` check additionally requires `rawPlan !== 'free'` — the exact condition that distinguishes a genuine Lifetime grant from the self-cancel signature. Locked by 2 explicit unit-test regressions (§97) plus, as of §98, **live confirmation through the real Admin UI** against dedicated genuine-FREE and expired-trial-FREE TEST personas (both markets) — every FREE-tier row renders with FREE's own gray styling, never PRO/Lifetime's violet+Crown. Admin's two plan-badge render sites (desktop table + mobile card) are now wired to `planCatalog.js` via `getPlanBadgeVisual()`, replacing the prior independent hardcoded ternary chains, zero visual change (§98). `UserDetailsModal.jsx`'s own badge color remains intentionally unwired (would change FREE's displayed color — a genuine visual change, out of scope). **New nuance surfaced by §98's real persona data**: a genuinely-admin-granted Lifetime BASIC account (`plan:'basic', trial_ends_at:null`) also resolves `isLifetime:true` under the current rule — correct today (no purchase flow exists; only the admin "Toggle Lifetime" action can produce this shape), but once real billing exists an ordinary non-trial paid subscriber will share this exact shape and be mislabeled Lifetime too — Lifetime will need its own explicit field (e.g. `admin_override`) once the `subscriptionStatus` axis (below) is built.

**Known live bugs, both addressed 2026-08-31 (`PROFLOW_PROJECT_CONTEXT.md` §98), committed locally not pushed, not deployed to Production**: (1) `send-trial-expiration-email`'s candidate filter required `plan==='free'` to consider a candidate, but a genuine active-trial account always has `plan==='pro'` (per `planEntitlements.js`'s own model) — the filter blocked its entire real target audience, unconditionally. **Fixed**: filter now requires `plan==='pro'`; the eligibility decision was extracted into a pure, dependency-free `eligibility.ts` shared by the Deno runtime and a new 14-case Vitest suite. (2) `send-subscription-expiration-email` queries 3 `business_settings` columns (`subscription_ends_at`, `subscription_reminder_3d_sent`, `subscription_reminder_24h_sent`) that do not exist anywhere in the live schema (confirmed against the Production-captured base-schema migration) — every batch invocation would throw. **No schema migration authorized/performed.** Fixed fail-safe per the Owner's explicit product rule (a healthy auto-renewing subscription must never get a routine "ending soon" email — only a genuine known-end lifecycle state like an approved cancel-at-period-end deserves one, and that axis doesn't exist yet): batch mode now short-circuits to an explicit, tested safe no-op response, documenting the exact future schema dependency at the call site. Neither fix has been deployed to Production or invoked against real data — both email functions remain deployed/ACTIVE in Production only, not on TEST. **Corrected 2026-08-31 (`PROFLOW_PROJECT_CONTEXT.md` §102) — this section previously said no scheduler "could be confirmed or ruled out"; that was resolved simply by reading a file already in the repo: a real Vercel Cron exists** (`vercel.json`'s `crons` entry, `/api/cron`, daily `0 8 * * *` UTC → `api/cron.js` → `triggerExpirationReminders()`), already on `origin/main`, already invoking both `send-trial-expiration-email` and `send-subscription-expiration-email` in `mode:'batch'` against Production's currently-deployed (unfixed) functions. Production has no `pg_cron` extension (confirmed, ruling out a DB-native path). **Deploying these fixes to Production Edge Functions requires separate, explicit Owner authorization** — not granted by this task (local-only work) — and, once deployed, the trial-email fix will cause the very next 08:00 UTC cron run to attempt real customer email sends to genuinely-eligible active-trial accounts, a consequence to weigh explicitly before authorizing that specific redeploy.

**Dependency-gap found and repaired, 2026-08-31 (`PROFLOW_PROJECT_CONTEXT.md` §99)**: `src/utils/accountEntitlement.js` (committed) imports `computeEffectivePlan` from `src/utils/planEntitlements.js` — which had never itself been committed on any ref (an untracked leftover of the separate, already-documented §51 Trial Expiration→FREE task). A clean `git archive`-exported, working-tree-isolated build proved the committed HEAD would fail on a fresh checkout as a result. Repaired by committing `planEntitlements.js`/`planEntitlements.test.js` exactly as already TEST-verified, no code change — re-proven buildable via the same isolated-export method afterward. The committed application tree is now self-contained.

**Local workstream consolidation, 2026-08-31 (`PROFLOW_PROJECT_CONTEXT.md` §100)**: 12 further local commits gave safe git checkpoints to the pieces of the remaining uncommitted diff that were coherent and safely separable on their own — the Item 17/18 SQL migration packages (still unapplied to Production), the base-schema-capture package, `dev:localtest`'s script and TEST fail-closed guard, the Order-Number-sorting fix, the Item 25 market-routing function, Item 23 Warranty's DB+API backend slice, variable-font infrastructure, the global print rule, and `PdfFileIcon.jsx`. Confirmed and left deliberately uncommitted: `Dashboard.jsx` (the entangled linchpin of the remaining threads) and everything whose correctness genuinely depends on it (`QuoteForm.jsx`, `QuotesTab.jsx`, `SettingsTab.jsx`, `PublicQuote.jsx`/`PublicQuoteEn.jsx`, `useSignaturePad.js` — the last confirmed as a real regression risk, since its new default-off signing-activation gate would silently break the *already-committed-and-pushed* `PublicQuote.jsx`/`PublicQuoteEn.jsx`'s signature pad if committed alone). `entry-server.jsx` stays uncommitted per its own explicit §68/§69 documentation. Still no push.

**Dashboard-centered bundle committed, 2026-08-31 (`PROFLOW_PROJECT_CONTEXT.md` §101)**: `Dashboard.jsx` and its 7 dependents (`QuoteForm.jsx`, `QuotesTab.jsx`/`.test.jsx`, `SettingsTab.jsx`, `PublicQuote.jsx`/`PublicQuoteEn.jsx`, `useSignaturePad.js`) committed together as one atomic unit (`fde680b`) after a full line-by-line diff reconstruction confirmed genuine, real runtime cross-file dependencies (traced props, not assumed) — the customer-facing Dashboard/Settings/QuoteForm surfaces now use the corrected `effectivePlan` resolver (closing the same class of bug already fixed in Admin), Warranty is wired end-to-end (Settings → Dashboard snapshot-at-creation → Public Quote display), the signature pad's tap-to-activate lifecycle is live on both `PublicQuote.jsx`/`PublicQuoteEn.jsx`, and the trial-status bar renders zero-layout-shift directly from `Dashboard.jsx`. TEST Browser QA used real simulated interaction (not just DOM inspection) — the signature pad's draw/clear/deactivate cycle was verified via actual canvas ink (`toDataURL()` byte-length change). **Pre-push audit finding**: of this task's changes, only Warranty introduces a genuine new Production risk — it's the first commit whose live code path reads/writes `default_warranty`/`warranty`, and that migration is not yet applied to Production. Everything else in the 18-commit chain (Attn, quote-number, market-routing, entitlement resolver, Admin V2, email fixes) was confirmed pushable without any DB action. Still no push.

**Production pre-push preflight, 2026-08-31 (`PROFLOW_PROJECT_CONTEXT.md` §102, read-only)**: independently confirmed via a schema-only Production dump plus targeted read-only SQL — `default_warranty`/`warranty` are genuinely MISSING from Production (not inferred from migration files alone), and confirmed the Warranty risk is more severe than previously stated: no fail-safe exists, so pushing would break *every* quote and Business Settings save, not just Warranty. `attn_name`/`attn_role` are also missing but already-live code handles this gracefully by design (confirmed PASS). Quote-number's A90 mechanism reconfirmed unchanged (native Postgres sequence default, zero app code). See the corrected scheduler finding above (real Vercel Cron, resolves the prior "unknown").

**Warranty migration source-reviewed, 2026-08-31 (`PROFLOW_PROJECT_CONTEXT.md` §103, read-only)**: `6430cf5`'s SQL classified MINIMAL (two additive `ADD COLUMN IF NOT EXISTS ... text` + two scoped grants + two comments, no unrelated statements), confirmed no naming collision, no dependent views, low lock risk (metadata-only Postgres 11+ operation), fully forward-safe on every failure path. **Ready for Owner authorization to apply**, not yet applied. See §1.A above for the separately-found, separately-scoped Vercel canonical-redirect gap this same task uncovered.

**Paid-subscription reminder-email policy, CLOSED 2026-08-31** (`PROFLOW_PROJECT_CONTEXT.md` §93): an ordinary auto-renewing BASIC/PRO subscription must **never** receive a 3-day/24-hour "ending soon" reminder at a normal monthly `current_period_end` — that boundary is a renewal, not an expiration. The reminder pattern applies **only** to `CANCEL_AT_PERIOD_END` (renewal disabled, entitlement genuinely ending; exact copy not yet finalized). Payment-failure notifications and renewal receipts/invoices are explicitly separate flows from expiration reminders. Trial reminders (3-day/24-hour before trial end) remain fully mandatory and unaffected by this correction — bug (3) above stays open, unfixed.

**Billing Sandbox requirement, permanent, documented 2026-08-31, not implemented** (`PROFLOW_PROJECT_CONTEXT.md` §93): the future payment-provider integration must support full end-to-end TEST simulation at the **real configured plan prices** — a special hidden Production ₪0 plan must never become the de facto test strategy, since it would leave the real price/checkout/webhook path permanently unexercised. Required scenarios: purchase BASIC/PRO, renewal success, payment failure, cancellation at period end, BASIC↔PRO upgrade/downgrade, webhook/event replay, duplicate-event idempotency, reconciliation — exercising the full chain (Checkout → provider webhook → canonical subscription state → entitlement resolver → Dashboard badge → Admin state → permissions/limits → reminders/emails) so Sandbox testing validates the same code path Production traffic uses.

## 17. Settings / Business Profile (CRM)

**DOCUMENTED**: `SettingsTab.jsx` manages business profile fields — name, tax ID, phone, currency, and a pipe-delimited `street|city|state|zip` address format (parsed for display elsewhere, e.g. `UserDetailsModal.jsx`'s `formatAddressCity()`). Plan/trial management and subscription cancellation flows live here and in `PricingModal.jsx`.

## 18. Where Current Rules Live

This file describes architecture, not workflow. For current authorization state, owner-approved workflow, the coding-agent (Claude) rules, commit/push discipline, the Local+International bilateral-regression requirement, David Aluminum protection, and the exact current project checkpoint — **see `PROFLOW_PROJECT_CONTEXT.md`, which is authoritative for all of that.** For detailed historical evidence behind any claim above, see `PROFLOW_HANDOFF.md`.

## 19. Documentation Persistence / Repository Access

GitHub (`quotecode-dev/quotecode-clean`) is this project's persistent, version-controlled source of truth for all six canonical project documents (§0.A/§0.B of `PROFLOW_PROJECT_CONTEXT.md`) and all application code. A ChatGPT session has a **verified, read-only** connector to this repository (owner-selected "Allow read actions" permission mode) — see `PROFLOW_PROJECT_CONTEXT.md` §26.A for the verification details and Protocol items 17-19 for the exact reading/freshness/write-authorization rules governing its use. This file is not the place for connector operating instructions — it exists only to note that GitHub, not chat memory or an uploaded copy, is the intended shared source between coding-agent work and future ChatGPT sessions.
